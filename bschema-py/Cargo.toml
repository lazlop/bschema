[package]
name = "bschema-py"
version = "0.1.0"
edition = "2024"

[lib]
name = "_bschema_rs"
crate-type = ["cdylib"]

[dependencies]
bschema-core = { version = "0.1.0", path = "../bschema-core" }
pyo3 = { version = "0.29.0", features = ["extension-module", "abi3-py311"] }
